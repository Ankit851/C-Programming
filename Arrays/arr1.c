#include<stdio.h>
int main()
{
    int arr[5] = {1,2,3,4,5};
    for(int i = 0 ; i < 5 ; i++ )
    {
        printf("The value at the index location %d is %d\n",i,arr[i]);
    }
    
    /*for(int j=0; j < 5 ; j++)
    {
        scanf("%d",&arr[j]);
    }
    */
}
